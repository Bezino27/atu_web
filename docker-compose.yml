version: "3.9"

services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://178.104.54.84:8000/api
    container_name: atu_frontend
    ports:
      - "3000:3000"
    restart: always
    environment:
      NEXT_PUBLIC_API_URL: http://178.104.54.84:8000/api
